# Estos ficheros son una foto regenerada en cada ciclo de escaneo (local o
# GitHub Actions), sin relacion de precedencia entre versiones - en un
# conflicto de merge basta con quedarse con la version del lado que esta
# mergeando ("ours") en vez de fallar y bloquear el push. Requiere
# `git config merge.ours.driver true` en cada maquina que escriba a este repo
# (ver deploy/README_DEPLOY.md, "Opcion C: scraper local").
data/active_opportunities.json merge=ours
docs/data.json merge=ours
data/surebets.db merge=ours -diff
